app = "backend-devops-interview"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "uv run --no-sync python manage.py migrate"
  strategy = "rolling"

[env]
  DEBUG = "False"
  ALLOWED_HOSTS = "backend-devops-interview.fly.dev"
  CSRF_TRUSTED_ORIGINS = "https://backend-devops-interview.fly.dev"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    interval = "15s"
    timeout = "5s"
    grace_period = "20s"
    method = "GET"
    path = "/healthz"
    headers = { Host = "backend-devops-interview.fly.dev" }
